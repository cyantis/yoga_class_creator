<%= form_for [teacher, lesson, note] do |f| %>
  <%= f.label :content, 'Take Note!' %></br>
  <%= f.text_area :content %></br>
  <%= hidden_field_tag 'note[lesson_id]', params[:lesson_id] %>
  <%= f.submit "Noted!"%>
<% end %>
